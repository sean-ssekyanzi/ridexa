const BACKEND = import.meta.env.VITE_BACKEND_URL || "https://app2-image-latest.onrender.com";
const STREAM_HOST = import.meta.env.VITE_STREAM_HOST || "https://vidsrc.pm";

export { STREAM_HOST };
export default BACKEND;
